# ⚠️ 敏感配置文件说明

**生成时间：** 2026-06-14 02:57  
**检查者：** 小悟同学（Phase 2.5 只读审计）

---

## 敏感文件清单

| 文件名 | 风险等级 | 敏感匹配数 | 内容类型 |
|--------|---------|-----------|---------|
| `feishu-credentials.json` | 🔴 **高** | 3 (token:2, secret:1) | 飞书 API 凭证，含 access_token / refresh_token 等 |
| `openclaw-backup.json` | 🔴 **高** | 5 (token:3, key:1, secret:1) | OpenClaw 备份配置，含 token、key、secret |
| `feishu-pairing.json` | 🟢 **低** | 0 | 飞书配对信息，未检出敏感关键词（但仍建议保护） |

---

## 安全规则

### ❌ 禁止操作

1. **不得进入普通备份** — 这些文件不应包含在 tar.gz / rsync 常规备份中
2. **不得提交 Git** — 需添加到 `.gitignore`（确认 `xiaowuOS/config/*.json` 已排除）
3. **不得同步到公开环境** — 禁止上传 Feishu 云文档、GitHub、公网存储
4. **不得打印内容** — AI/脚本不输出文件全文，仅报告文件名和风险等级

### ✅ 允许操作

1. **只读审计** — 检查敏感关键词匹配数
2. **权限收紧** — 确认文件权限为 `600`（仅 owner 可读写）
3. **迁移到安全位置** — 如需备份，加密后存入 `/home/john/.xiaowuOS/config/`

---

## 当前状态

| 检查项 | 状态 |
|--------|------|
| 文件是否已删除 | ❌ 否（保留原位） |
| 文件是否已移动 | ❌ 否（保留原位） |
| README_SENSITIVE.md 是否已生成 | ✅ 是 |
| 运行时配置是否修改 | ❌ 否 |

---

*如需进一步处理这些敏感文件，请联系澄木老师。*
